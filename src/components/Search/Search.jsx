

function Search({ text, setText, className }) {

    const handleChange = () => {
        /**
         * TODO
         */
    }
    return(
        <input className={className} onChange={handleChange} />
    )
}

export default Search